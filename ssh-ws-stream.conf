# Deprecated reference only.
#
# Do not enable a separate stream listener for this project. The Python bridge
# owns public TCP port 80 and must receive HTTP headers directly so it can
# distinguish CONNECT from WebSocket Upgrade requests.