# Deprecated reference only.
#
# CONNECT is handled directly by sshws_server.py on public port 80.
# Do not enable a separate proxy in front of it: this service intentionally
# keeps the SSH backend fixed to the local daemon.